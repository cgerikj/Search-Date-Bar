{
  "name": "search-date-bar",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright test harness for the Search Date Bar Chrome extension",
  "scripts": {
    "test": "playwright test tests",
    "release": "node scripts/release.js",
    "generate-icon": "node scripts/generate-icon.js",
    "generate-screenshots": "node scripts/generate-screenshots.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^26.1.1",
    "archiver": "^6.0.2",
    "sharp": "^0.35.3"
  }
}
